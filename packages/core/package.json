{
  "name": "@shipshape/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Pure domain: rubric scoring, board ordering, portfolio rollups. No I/O, no env, no database.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --test 'test/*.test.ts'"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "@types/node": "^24.3.0"
  }
}
